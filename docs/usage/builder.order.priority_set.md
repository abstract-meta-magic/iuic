# priority_set(...)

## Description
Sets the element's priority within its layer.

## Behavior
...

## Use-case
Use when you need to change the element's position within its layer.

## Example

```c++

// priority = 0
b.order.priority_set(100);
// priority = 100

```
