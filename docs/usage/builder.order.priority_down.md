# priority_down()

## Description
Decrements the element's priority within its layer.

## Behavior
- Decrements the current element's priority within its layer by one.

## Use-case
Use when you need to change the element's position within its layer.

## Example

```c++

// priority = 0
b.order.priority_up();
// priority = -1

```
