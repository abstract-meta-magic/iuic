# layer_set(...)

## Description
Sets the layer value.

## Behavior
- Set the current element's layer.

## Use-case
Use when you need to change the element's position in the global rendering order.

## Example

```c++

// layer = 0
b.order.layer_set(100);
// layer = 100

```
