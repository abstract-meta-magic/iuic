
## Ответственность 
// Прописать конкретнее
# event
Используется для прикрепления реакций на события к элементам.
## Ограничения
- событием может быть указателем на функцию или лямбду без захвата, которые принимают [[объект события]].
- захват объектов происходит через [[storage#ключ|ключи]]. Только один общий объект, и только один текстовый буфер.
- зависимость от [[policy|политик]].
## Объект события
## Политики
## Захват объектов
Возможен только через получение [[storage#ключ|ключей]] и передачи их в **события**.
```cpp 
object_registry_key ork = ...; // он же ork_t
text_registry_key trk = ...;   // он же trk_t 

b.event([](/* event_object */){ ... },trk);
b.event([](/* event_object */){ ... },ork);
b.event([](/* event_object */){ ... },ork,trk);
```
Тут можно описать причины такого проектирования.


>[!INFO] uid_t

тут нужно написать что события берет uid элемента к которому привязывается 


## Пример
Ссылки:
1. [[builder/uid]]
2. [[memory]]
3. [[объект события]]
```c++
ctx.make([](context::builder &b){

  uid_t uid = ...; // получение uid (1)

  // использование uid для ветвления элемента,
  // чтобы предать событию уникальности
  b.element.frame(uid,[](context::builder b&){
    // требуеться для локальных событий
    b.policy.hovered(policy::hovered::propagate);

	
	tkr_t trk = ...; // получение trk (2)

    b.event([](/*event_object*/) {
	  // что-то происходит (3)
    },trk);
  });
});
```